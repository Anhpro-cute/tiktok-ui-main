const images = {
    logo: require('~/assets/images/logo.svg').default,
    noImage: require('~/assets/images/no-image.png'), //file png không cần default như svg; Xây dựng UI phần Header #7
};

export default images;
