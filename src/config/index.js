import routes from './routes';
// Tái cấu trúc và tối ưu code #1: routes config
// tuy không có ý nghĩa nhiều nhưng nên có file index này để sau có nhiều config cho vào đây
const config = {
    routes,
};

export default config;
