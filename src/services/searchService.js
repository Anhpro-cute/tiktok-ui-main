import * as httpRequest from '~/utils/httpRequest'; // Tìm hiểu và sử dụng thư viện Axios

export const search = async (q, type = 'less') => {
    try {
        const res = await httpRequest.get('users/search', {
            params: {
                q,
                type,
            },
        });
        return res.data;
    } catch (error) {
        console.log(error);
    }
};
